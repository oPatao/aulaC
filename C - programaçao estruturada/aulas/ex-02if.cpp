/*caucular a força dogitando a massa e aceleração e podera também, calcular a area e o o
perimetro de um circulo, digitando o raio. Utilize a estrutura switch, e do_while*/

#include <stdio.h>
#include <stdlib.h>

int main (){

    int i, sim;
    float forca, massa, aceleracao, raio, perimetroCirculo, areaCirculo; 

    sim = 1;

    while (sim == 1)
    {
        switch (i)
        {
        case 1:
            printf("qual o valor da forca");

            break;
        
        default:
            break;
        }
    }
    

    system("pause");
}